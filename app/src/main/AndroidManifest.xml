<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetPackCompose"
        tools:targetApi="31">
        <activity
            android:name=".ui.activity.camera.CameraActivity"
            android:exported="false"
            android:label="@string/title_activity_camera"
            android:theme="@style/Theme.JetPackCompose" />
        <activity
            android:name=".ui.activity.ui.NavigationController"
            android:exported="false"
            android:label="@string/title_activity_navigation_controller"
            android:theme="@style/Theme.JetPackCompose" />
        <activity
            android:name=".ui.activity.FormActivity"
            android:exported="false"
            android:label="@string/title_activity_form"
            android:theme="@style/Theme.JetPackCompose" />
        <activity
            android:name=".ui.activity.RecycleViewActivity"
            android:exported="false"
            android:label="@string/title_activity_recycle_view"
            android:theme="@style/Theme.JetPackCompose" />
        <activity
            android:name=".ui.activity.TextActivity"
            android:exported="false"
            android:label="@string/title_activity_text"
            android:theme="@style/Theme.JetPackCompose" />
        <activity
            android:name=".ui.activity.ButtonsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetPackCompose">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>